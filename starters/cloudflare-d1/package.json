{
  "name": "nimiq-cloudflare-d1",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc",
    "deploy": "wrangler deploy",
    "test": "echo 'Skipping: @cloudflare/vitest-pool-workers does not support vitest 4 yet'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nimiq/core": "^2.2.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.10.0",
    "@cloudflare/workers-types": "^4.20251014.0",
    "@types/node": "^24.9.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.3",
    "wrangler": "^4.45.0"
  }
}
